name: Release Please

on:
  workflow_dispatch:
  push:
    branches:
      - main

concurrency:
  group: release-please-${{ github.ref }}

permissions:
  contents: read

jobs:
  release-please:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    outputs:
      release_created: ${{ steps.release.outputs.release_created }}
      tag_name: ${{ steps.release.outputs.tag_name }}
    steps:
      - name: Release Please
        id: release
        uses: googleapis/release-please-action@v5
        with:
          token: ${{ secrets.RELEASE_PLEASE_TOKEN || secrets.GITHUB_TOKEN }}
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json

  # Production Deployment - only runs when release-please PR is merged
  deploy-production:
    needs: release-please
    runs-on: ubuntu-latest
    if: ${{ needs.release-please.outputs.release_created == 'true' }}
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          token: ${{ secrets.RELEASE_PLEASE_TOKEN || secrets.GITHUB_TOKEN }}

      # Apply pending DB migrations BEFORE pushing to the production branch, so
      # the schema is ready when Vercel builds the new production deployment.
      # drizzle-kit migrate is idempotent (journal-tracked), so this is a fast
      # no-op on releases that don't touch packages/db. Requires the
      # DATABASE_URL repo secret (prod connection).
      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: "20"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run database migrations
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: npm run db:migrate
        working-directory: packages/db

      - name: Setup Git User
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"

      - name: Merge main into production branch
        run: |
          set -e
          # Fetch remote production branch if it exists
          git fetch origin production || true
          # Checkout production: track remote if exists, else create from main
          if git show-ref --verify --quiet refs/remotes/origin/production; then
            git checkout --track origin/production || git checkout production
          else
            git checkout -b production main
          fi
          # Merge main with theirs strategy to handle any conflicts
          git merge --no-ff -X theirs main -m "chore: deploy release ${{ needs.release-please.outputs.tag_name }} to production"
          git push origin production
