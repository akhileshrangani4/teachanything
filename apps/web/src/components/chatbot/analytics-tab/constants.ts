export const COMMON_QUESTIONS_PAGE_SIZE = 5;

export const chartColors = {
  grid: "var(--muted)",
  tick: "var(--muted-foreground)",
  card: "var(--card)",
  border: "var(--border)",
  primary: "var(--primary)",
  sessions: "#0f766e",
} as const;
